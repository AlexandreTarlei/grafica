import { ClientsPage } from '@/modules/signage/clients/pages/ClientsPage'

/** CRM evoluído para clientes corporativos signage (mantém rota /admin/crm). */
export default function CrmModulePage() {
  return <ClientsPage />
}
